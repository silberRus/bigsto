
Функция ДоступнаРоль(Роль, ПолныеПраваБольшеНеДействуют = Ложь) Экспорт
	
	// проверяет доступна роль или нет по БП для текущего пользователя
		
	Если Не ПолныеПраваБольшеНеДействуют И РольДоступна("ПолныеПрава") Тогда
		
		Возврат Истина;
		
	Иначе
	
		Запрос = Новый Запрос("
		|ВЫБРАТЬ ПЕРВЫЕ 1 ИСТИНА 
		|ИЗ		РегистрСведений.ИсполнителиЗадач
		|ГДЕ	РольИсполнителя = &Роль И
		|		Исполнитель 	= &Пользователь
		|");
		
		Запрос.УстановитьПараметр("Роль", 			Роль);
		Запрос.УстановитьПараметр("Пользователь", 	ПараметрыСеанса.ТекущийПользователь);
		
		Возврат Не Запрос.Выполнить().Пустой();
		
	КонецЕсли;
	
КонецФункции

Функция ЛогинЛаксимо() Экспорт
	
	Возврат "ru885376";
	
КонецФункции
Функция ПарольЛаксимо() Экспорт
	
	Возврат "iuz7iM3ud_0jfiy";
	
КонецФункции

Функция СвойствоНомерССайта() Экспорт
	
	Возврат ПланыВидовХарактеристик.ДополнительныеРеквизитыИСведения.ПолучитьСсылку(Новый УникальныйИдентификатор("b2d9eaba-e1d0-11e5-80d9-00155dfa1a02"));
	
КонецФункции

Функция УзелОбменаПоЗаказам() Экспорт
	
	Возврат ПланыОбмена.ОбменССайтом.ПолучитьСсылку(Новый УникальныйИдентификатор("73d5bb46-9264-11e5-80c0-003067e83ad5"));
	
КонецФункции

Функция МожетПолучатьДеньгиОтВодителей() Экспорт
	
	// Возвращает истина если может получать деньги от водителей
	
	// в будущем заменить поиск по наименованию
	Возврат ДоступнаРоль(Справочники.РолиИсполнителей.НайтиПоНаименованию("ДеньгиОтВодителей"), Истина);
	
КонецФункции

Функция ПолучитьМассивРегионов() Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗЛИЧНЫЕ Наименование ИЗ РегистрСведений.АдресныеОбъекты ГДЕ Уровень = 1 УПОРЯДОЧИТЬ ПО Наименование");
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Наименование");
	
КонецФункции

Функция ИмяПользователяИБстр() Экспорт
	
	Возврат Строка(Пользователи.ТекущийПользователь());
	
КонецФункции
