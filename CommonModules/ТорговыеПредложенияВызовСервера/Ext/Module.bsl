////////////////////////////////////////////////////////////////////////////////
// Подсистема "Торговые предложения".
// ОбщийМодуль.ТорговыеПредложенияВызовСервера.
////////////////////////////////////////////////////////////////////////////////

#Область СлужебныйПрограммныйИнтерфейс

// Запускает процедуру синхронизации торговых предложений в фоновом режиме.
//
// Возвращаемое значение:
//   ДлительнаяОперация - структура данных длительной операции.
//
Функция СинхронизироватьТорговыеПредложенияВФоне() Экспорт 

	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(Новый УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Синхронизация торговых предложений с сервисом 1С:Бизнес-сеть'");
	
	Возврат ДлительныеОперации.ВыполнитьВФоне("ТорговыеПредложения.СинхронизацияТорговыхПредложений", 
		Неопределено, ПараметрыВыполнения);
		
КонецФункции

// Обработка переопределяемого метода.
//
// Параметры:
//  ИмяМетода				 - Строка - наименование метода.
//  ДополнительныеПараметры	 - Структура - параметры метода.
// 
// Возвращаемое значение:
//  Произвольный - возвращаемое значение переопределяемого метода.
//
Функция ОбработатьПереопределяемыйМетод(ИмяМетода, ДополнительныеПараметры = Неопределено) Экспорт
	
	// Вызов метода в переопределяемом общем модуле.
	Возврат ТорговыеПредложенияПереопределяемый.ОбработатьПереопределяемыйМетод(ИмяМетода, ДополнительныеПараметры);
	
КонецФункции

#КонецОбласти
