
Процедура ВыбранФайл(ПомещенныеФайлы, ДопПараметры) Экспорт
	
	Если ТипЗнч(ПомещенныеФайлы) = Тип("Массив") И ПомещенныеФайлы.Количество() Тогда
		
		Результат = АТ_Сканер.РаспознатьТекст(ПомещенныеФайлы[0].Хранение);
		ЗаполнитьЗначенияСвойств(ДопПараметры.Форма, Результат);
		
	КонецЕсли;
	
КонецПроцедуры	
Процедура НачатьРаспознованиеРеквизитов(Форма) Экспорт
	
	Если АТ_Сканер.ПравилДляФормыНет(Форма.ИмяФормы) Тогда
		ОбработатьСозданиеПервыхПравилФормы(Форма.ИмяФормы);
	Иначе
		
		ДВ = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
		ДВ.МножественныйВыбор 	= Ложь;
		ДВ.Фильтр 				= "PNG|*.png";
	
		НачатьПомещениеФайлов(Новый ОписаниеОповещения("ВыбранФайл", АТ_СканерКлиент, новый Структура("Форма", Форма)),,ДВ, Истина);
		
	КонецЕсли;

КонецПроцедуры

Процедура ОбработатьСозданиеПервыхПравилФормы(ИмяФормы)
	
	ПоказатьВопрос(Новый ОписаниеОповещения("СоздатьНовыеПравилаОтвет", АТ_СканерКлиент, Новый Структура("ИмяФормы", ИмяФормы)),"Нет правил распознования для этой формы, 
					|Создать первое правило?", РежимДиалогаВопрос.ДаНет);	
КонецПроцедуры
Процедура СоздатьНовыеПравилаОтвет(Ответ, ДопПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Да Тогда		
		ОткрытьФорму("Справочник.АТ_ПравилаЗаполненияРаспознанного.ФормаОбъекта", ДопПараметры);		
	КонецЕсли;
	
КонецПроцедуры

Процедура ИграИлиНеИгра() Экспорт
	
	Если АТ_Сканер.ИграМожетНачаться() Тогда
		
		ОткрытьФорму("Обработка.АТ_ДобраяОбработка.Форма.ВамБонус");
		
	КонецЕсли;
	
КонецПроцедуры