
Процедура ПередЗаписью(Отказ)
	
	Если НЕ ЭтоНовый() тогда
		
		Изменил 						= ПараметрыСеанса.ТекущийПользователь;
		ДатаМодификацииУниверсальная 	= ТекущаяДата();
		
	КонецЕсли;
	
	//Регистрируем изменение товаров	
	лОбъект = Номенклатура.ПолучитьОбъект();
	лОбъект.Записать();
	
	//Регистрируем изменение характеристики	
	лОбъект = ХарактеристикаНоменклатуры.ПолучитьОбъект();
	лОбъект.Записать();
	
КонецПроцедуры
