
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ОшибкаЧтения 	= Ложь;
	ТекстДок 		= Новый ТекстовыйДокумент;
	ИмяФайла		= АТ_Клиент.ПолучитьИмяIPФайлаНаСервере();
	
	ТекстДок.УстановитьТекст(АТ_ОбщегоНазначения.ПолучитьТекст(ИмяФайла, ОшибкаЧтения));
	
	Если ОшибкаЧтения Тогда
		ПоказатьПредупреждение(,"Ошибка чтения файла на сервере: " + ИмяФайла);
	Иначе
		ТекстДок.Показать("ip адреса клиентов");
	КонецЕсли;
	
КонецПроцедуры
