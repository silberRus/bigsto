Функция ВернутьОтвет(Запрос, Ответ) 
	
	Если Константы.ВестиЛогЗапросовRest.Получить() Тогда
	
		HTTPМодуль.ЛогЗапроса(Запрос); 
		HTTPМодуль.ЛогОтвета(Ответ); 
		
	КонецЕсли;
	
	Возврат Ответ;
	
КонецФункции


Функция ПолучитьОстаткиGET(Запрос)	Возврат ВернутьОтвет(Запрос, АТ_AccAPI.ПолучитьОстаткиGET(Запрос)) КонецФункции