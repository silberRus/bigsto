
&НаСервере
Функция ПолучитьДоступныеСклады(Партнер)
	
	Возврат АТ_Сервер.ПолучитьДоступныеСкладыПоПартнеру(Партнер);
	
КонецФункции

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Текст = Новый ТекстовыйДокумент;
	Текст.УстановитьТекст("Склады которые доступны " + ПараметрКоманды + ":
		|" + СтрСоединить(ПолучитьДоступныеСклады(ПараметрКоманды), Символы.ПС));
	
	Текст.Показать();
	
КонецПроцедуры
