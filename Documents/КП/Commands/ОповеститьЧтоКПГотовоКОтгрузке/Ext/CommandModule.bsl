
&НаСервере
Процедура ОповеститьНаСервере(КПСсылка)
	
	АТ_Сервер.ОповеститьМенеджераЧтоКПГотов(КПСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ОповеститьНаСервере(ПараметрКоманды);
	ПоказатьОповещениеПользователя("Оповещение создано",, ПараметрКоманды, БиблиотекаКартинок.СообщениеВТехПоддержку);
	
КонецПроцедуры
