
Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Данные.НомерРасписки) Тогда
		
		СтандартнаяОбработка = Ложь;
		Представление = СтрШаблон("Экспедиторская №%1 от %2", Данные.НомерРасписки, Формат(?(Данные.ДатаРасписки = '00010101', Данные.Дата, Данные.ДатаРасписки), "ДФ=d.M.yy"));
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	Поля = СтрРазделить("Номер,Дата,Ссылка,НомерРасписки,ДатаРасписки", ",");
	
КонецПроцедуры
