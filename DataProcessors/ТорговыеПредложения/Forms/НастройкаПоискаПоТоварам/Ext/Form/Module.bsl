
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Параметры.Свойство("ОграничениеТорговыхПредложений", ОграничениеТорговыхПредложений);
	Параметры.Свойство("ПревышениеМинимальнойЦены",      ПревышениеМинимальнойЦены);
	Параметры.Свойство("МаксимальныйСрокПоставки",       МаксимальныйСрокПоставки);
	Параметры.Свойство("ПоискПоНаименованию",            ПоискПоНаименованию);
	Параметры.Свойство("ПоискПоАртикулу",                ПоискПоАртикулу);
	Параметры.Свойство("ПоискПоШтрихКоду",               ПоискПоШтрихКоду);
	Параметры.Свойство("ПоискПоХарактеристике",          ПоискПоХарактеристике);
	Элементы.ПоискПоХарактеристике.Доступность = ПоискПоНаименованию;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ИзменениеРежимаПоиска(Элемент)
	
	Имя = Элемент.Имя;
	Если Не ПоискПоНаименованию И Не ПоискПоАртикулу И Не ПоискПоШтрихКоду Тогда
		ЭтотОбъект[Имя] = Не ЭтотОбъект[Имя];
		Возврат;
	КонецЕсли;
	
	Элементы.ПоискПоХарактеристике.Доступность = ПоискПоНаименованию;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаПрименить(Команда)
	
	ПараметрыВозврата = Новый Структура;
	ПараметрыВозврата.Вставить("ОграничениеТорговыхПредложений", ОграничениеТорговыхПредложений);
	ПараметрыВозврата.Вставить("ПревышениеМинимальнойЦены",      ПревышениеМинимальнойЦены);
	ПараметрыВозврата.Вставить("МаксимальныйСрокПоставки",       МаксимальныйСрокПоставки);
	ПараметрыВозврата.Вставить("ПоискПоНаименованию",            ПоискПоНаименованию);
	ПараметрыВозврата.Вставить("ПоискПоАртикулу",                ПоискПоАртикулу);
	ПараметрыВозврата.Вставить("ПоискПоШтрихКоду",               ПоискПоШтрихКоду);
	ПараметрыВозврата.Вставить("ПоискПоХарактеристике",          ПоискПоХарактеристике);
	Закрыть(ПараметрыВозврата);
	
КонецПроцедуры

#КонецОбласти
